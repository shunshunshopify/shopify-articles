query BlogArticles($id: ID!) {
  blog(id: $id) {
    id
    handle
    articles(first: 50) {
      nodes {
        id
        title
        handle
        isPublished
        body
      }
    }
  }
}
